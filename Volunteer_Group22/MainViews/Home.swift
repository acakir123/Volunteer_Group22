// Template Home Page
// This is where the user reaches once signed in
// Will need to change where they go based on whether they are admin or normal user
// This will work for now

import SwiftUI

struct HomeView: View {
    var body: some View {
        Text("Welcome to the Home Page!")
    }
}
